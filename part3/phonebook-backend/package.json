{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "shx rm -rf dist && cd ../phonebook-frontend && npm run build && cp -r dist ../phonebook-backend",
    "deploy:full": "shx npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Devon Sasaki",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.1",
    "shx": "^0.3.4"
  }
}
